import { Meta } from '@storybook/addon-docs/blocks';
import { PageIntro, DoDont, TokenChip } from './DocBlocks';
import { AssetGrid, AssetTile, ClearSpace } from './BrandBlocks';
import lockupLight from '../assets/brand/bella-lockup-light.svg';
import lockupDark from '../assets/brand/bella-lockup-dark.svg';
import markIris from '../assets/brand/bella-mark-iris.svg';
import markLavender from '../assets/brand/bella-mark-lavender.svg';
import markInk from '../assets/brand/bella-mark-ink.svg';

<Meta title="Foundations/Logo" />

<PageIntro title="Logo">
  Bella in one line: floppy ear up, eyes closed, mid-grin. The mark always
  sits in front of the name.
</PageIntro>

## The lockup

Use it wherever BELLA introduces itself: this sidebar, the design-system page
header, slides. The version is set as live text next to it, never baked into
the file.

<AssetGrid columns={2}>
  <AssetTile src={lockupLight} alt="BELLA lockup on ground" ground="ground" label="On ground" height={110} download="bella-lockup-light.svg" />
  <AssetTile src={lockupDark} alt="BELLA lockup on navy" ground="navy" label="On navy" height={110} download="bella-lockup-dark.svg" />
</AssetGrid>

## The mark

On its own when space is tight: favicon, avatar, loader. One colour, never two.

<AssetGrid>
  <AssetTile src={markIris} alt="Mark in iris" ground="ground" label={<TokenChip token="color.brand.iris" />} download="bella-mark-iris.svg" />
  <AssetTile src={markLavender} alt="Mark in periwinkle" ground="navy" label={<TokenChip token="color.brand.periwinkle" />} download="bella-mark-lavender.svg" />
  <AssetTile src={markInk} alt="Mark in ink" ground="paper" label={<TokenChip token="color.brand.ink" />} download="bella-mark-ink.svg" />
</AssetGrid>

## Clear space and size

Keep one ear-height of space all round. The mark stops reading below 16px;
the lockup below 96px wide.

<ClearSpace src={lockupLight} alt="Lockup with clear space" />

## Do and don't

<DoDont
  doItems={[
    'Iris on light grounds, periwinkle on navy',
    'Mark in front of the name',
    'Scale the SVG, never a PNG',
  ]}
  dontItems={[
    'Flip Bella to face right',
    'Fill the outline or add a second colour',
    'Put the mark on the elleta site header (that one is the wordmark)',
  ]}
/>
