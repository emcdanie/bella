import { Meta } from '@storybook/addon-docs/blocks';
import { PageIntro, DoDont } from './DocBlocks';
import { AssetGrid, AssetTile } from './BrandBlocks';
import BrandWordmark from '../components/BrandWordmark/BrandWordmark';
import favicon from '../assets/brand/favicon/favicon.svg';

<Meta title="Brand/Logo" />

<PageIntro title="Logo">
  The logo is the wordmark: ELLETA for the site, BELLA for the system, drawn
  in the brand's own letters and cut through with the pattern. The E alone
  is the favicon.
</PageIntro>

## Which mark, where

<AssetGrid columns={2}>
  <AssetTile ground="light" label="ELLETA · the site nav and footer">
    <BrandWordmark word="ELLETA" size="nav" />
  </AssetTile>
  <AssetTile ground="light" label="BELLA · the system, this Storybook, the repo">
    <BrandWordmark word="BELLA" size="nav" />
  </AssetTile>
  <AssetTile ground="dark" label="Ink, on dark: where the pattern would clash">
    <BrandWordmark word="ELLETA" size="nav" variant="ink" />
  </AssetTile>
  <AssetTile src={favicon} alt="The favicon: the pattern E on cream" ground="panel" label="The E: favicon and app icon" height={64} download="favicon.svg" />
</AssetGrid>

The full rules for each live on their own pages: [Wordmark](?path=/docs/brand-wordmark--docs),
[Pattern](?path=/docs/brand-pattern--docs) and [Favicon](?path=/docs/brand-favicon--docs).

## Retired

The line-drawn dog lockups and marks (2026-07) are retired from the brand
(2026-09-22). The SVG files stay in `src/assets/brand/` for old consumers,
each marked deprecated; nothing new should use them.

## Do and don't

<DoDont
  doItems={[
    'Use the wordmark component, never a screenshot of it',
    'ELLETA for the site, BELLA for the system',
    'The E alone only as the favicon or app icon',
  ]}
  dontItems={[
    "Don't put the wordmark below 32px tall",
    "Don't use the retired dog lockups",
    "Don't set the name in another typeface as a logo",
  ]}
/>
